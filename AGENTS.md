# All-in-One Project

> Codex 大型代码库最佳实践配置

---

## 核心原则

Codex 像软件工程师一样浏览代码库：遍历文件系统、读取文件、使用 grep 精确查找内容。它在本地运行，无需维护代码库索引。

**Agentic search 的关键**：上下文质量决定导航质量。投资代码库设置（AGENTS.md + Skills）会带来更好的结果。

---

## Harness 扩展点（按优先级排序）

| 组件 | 作用 | 加载时机 |
|------|------|----------|
| **AGENTS.md** | 上下文文件，根目录+子目录分层 | 每个会话自动加载 |
| **Hooks** | 持续改进，动态上下文加载，自动检查 | 会话期间触发 |
| **Skills** | 按需加载专业工作流，避免污染上下文 | 任务触发 |
| **Plugins** | 分发最佳实践到团队 | 安装时 |
| **MCP Servers** | 连接内部工具、数据源、API | 按需调用 |
| **LSP 集成** | 符号级精确导航（符号跳转、引用查找） | 自动 |
| **Subagents** | 将探索与编辑分离 | 按需创建 |

---

## 三种配置模式

### 1. 使代码库可导航

- **AGENTS.md 精简分层**
  - 根文件：全局概览、指针、关键注意事项
  - 子目录文件：本地约定和命令
  - 每个子目录可有自己的 AGENTS.md

- **子目录初始化**
  - Codex 会自动向上遍历目录树加载所有 AGENTS.md
  - 作用域到相关部分，避免上下文膨胀

- **命令作用域到子目录**
  - 各子目录指定各自的测试和构建命令
  - 避免全量运行导致超时

- **.claudeignore 配置**
  - 在 `.Codex/settings.json` 中设置 `permissions.deny`
  - 版本控制，排除生成文件、构建产物、第三方代码
  - 代码生成器开发者可覆盖本地设置

- **代码库地图**
  - 非标准结构时，在根目录创建 markdown 索引
  - 顶层文件夹+一句话描述，作为 Codex 的目录

- **LSP 集成**
  - Grep 常见函数名返回数千匹配
  - LSP 返回同一符号的引用，过滤在读取前完成

### 2. 主动维护配置

- **定期审查（每3-6个月）**
  - 模型进化后，旧指令可能变得不必要或限制新模型
  - 例如：单文件变更规则对新模型可能是约束

- **检查触发时机**
  - 重大模型发布后性能停滞时
  - Hook 和 Skills 补偿特定模型限制后

### 3. 所有权分配

- **DRI 模式**（最小可行版本）
  - 一人负责配置、设置、权限策略、插件市场、AGENTS.md 约定

- **团队模式**（推荐）
  - 开发者体验/开发者效能团队负责
  - 新兴角色：Agent Manager（PM+工程师混合）

- **分阶段推广**
  - 先小团队验证，再逐步扩大
  - 第一天就让开发者有生产力

---

## 启动清单

```
□ 安装代码智能插件和对应语言服务器
□ 创建根目录 AGENTS.md（全局概览）
□ 各子目录添加 AGENTS.md（本地约定）
□ 配置 .Codex/settings.json（权限和排除规则）
□ 设置 LSP 服务器
□ 确定 DRI 或团队负责
□ 建立代码审查流程
□ 定义批准的 Skills 和 Plugins
```

---

## 常见错误

- 根 AGENTS.md 过于臃肿 → 移动到子目录
- 全量测试运行 → 子目录作用域命令
- 无 LSP 集成 → Grep 匹配不精确
- AGENTS.md 从不更新 → 每3-6个月审查

---

*来源：[Codex 在大型代码库中如何工作](https://Codex.com/blog/how-Codex-works-in-large-codebases) - 2026/05/14*